/*
 *  &  연산자는 비트연산도하고 논리연산도한다
 *  | 연산자는 비트연산
 */
public class Operatortest_06 {

	public static void main(String[] args) {
		int x=10,y=22;
		
		// x값이 양수이고 2의 배수이면 true 가 출력
		if (x>0 && x%2==0) 
			System.out.println(true); // if문에 실행문이 하나만 잇으면 {} 생략가능
		else 
			System.out.println(false);
		
		// x값이 양수이고 2의 배수이면 true 가 출력
		if (x>0 && x%2==0) 
			System.out.println(true); // if문에 실행문이 하나만 잇으면 {} 생략가능
		else 
			System.out.println(false);
		
	}

}
